<!-- HTML 구조 -->
<div class="hud-container">
  
  <!-- 배경 글리치 효과 -->
  <div class="hud-glitch-bg"></div>

  <!-- 좌측 패널: 네바 생체 신호 모니터링 (파동) -->
  <div class="panel left-panel">
    <div class="panel-header">[ BIO_SYNC: Neva_West ]</div>
    
    <div class="wave-container">
      <div class="wave-line wave-neva"></div>
      <div class="wave-line wave-marie"></div>
      <!-- 두 파동이 겹칠 때마다 노이즈 발생 -->
      <div class="merge-conflict-warning">MERGE CONFLICT DETECTED</div>
    </div>
    
    <div class="data-block">
      <p>HR: <span class="blink">72 bpm</span> (Stable)</p>
      <p>Temp: 36.8°C</p>
      <p>Cortisol: <span class="text-alert">Elevated</span></p>
      <p>Neural_Link: <span class="text-green">Standby (Airgap)</span></p>
    </div>
    
    <div class="nav-links mt-4">
      <p class="text-xs text-gray-500 mb-1">// ACCESS_LOG</p>
      <a href="/novels" class="hud-btn">> [READ_NOVELS]</a>
      <a href="/gallery" class="hud-btn">> [VIEW_ARCHIVE]</a>
    </div>
  </div>

  <!-- 중앙: 메인 시야 (여백) & 타겟팅 -->
  <div class="center-view">
    <div class="targeting-reticle">
      <div class="crosshair"></div>
      <div class="reticle-text">TARGET_LOCKED</div>
    </div>
  </div>

  <!-- 우측 패널: 백그라운드 태스크 및 시스템 로그 -->
  <div class="panel right-panel">
    <div class="panel-header">[ BACKGROUND_TASKS ]</div>
    
    <ul class="task-list">
      <li class="task-done">[✓] 함선 블랙박스 교체 및 도색 세탁</li>
      <li class="task-done">[✓] 식품 재조합기 레시피 업데이트 (버전 48)</li>
      <li class="task-active">[▶] 네바 호르몬 사이클 및 수면 패턴 분석 중...</li>
      <li class="task-active">[▶] 에우린 메인 서버 양자 해킹 시나리오 대기 중...</li>
      <li class="task-wait">[■] 캐스 제거 조건 재계산 (현재 조건 충족: 1/5)</li>
    </ul>

    <div class="terminal-log mt-4">
      <p class="text-xs text-gray-500 mb-1">// SYSTEM_LOG</p>
      <div class="typewriter-text">
        <p>> Warning: Unregistered access detected.</p>
        <p>> Bypassing security protocol...</p>
        <p>> <span class="text-alert">"네바가 좋아하는 걸 저만 놓치면 손해잖아요."</span></p>
      </div>
    </div>
    
    <div class="copyright-info mt-4 text-xs text-gray-600">
      <p>ALL DATA ENCRYPTED & CLASSIFIED</p>
      <p>UNAUTHORIZED ACCESS WILL BE TERMINATED</p>
      <p>&copy; 2026 Merge Conflict. All rights reserved.</p>
    </div>
  </div>

</div>

<style>
/* CSS 스타일링 (Tailwind CSS와 병행 사용 가능) */
:root {
  --hud-color: #0ff; /* 청록색 (마리) */
  --alert-color: #f00; /* 붉은색 (경고/네바) */
  --bg-dark: #050505;
}

body {
  background-color: var(--bg-dark);
  color: var(--hud-color);
  font-family: 'Courier New', Courier, monospace; /* 터미널 폰트 */
  margin: 0;
  overflow: hidden; /* 스크롤 방지하여 헬멧 뷰 느낌 강조 */
}

.hud-container {
  display: flex;
  justify-content: space-between;
  height: 100vh;
  padding: 20px;
  box-sizing: border-box;
  position: relative;
  /* 브라운관 느낌의 스캔라인 효과 */
  background: linear-gradient(rgba(18, 16, 16, 0) 50%, rgba(0, 0, 0, 0.25) 50%), linear-gradient(90deg, rgba(255, 0, 0, 0.06), rgba(0, 255, 0, 0.02), rgba(0, 0, 255, 0.06));
  background-size: 100% 2px, 3px 100%;
}

.panel {
  width: 25%;
  background: rgba(0, 255, 255, 0.05);
  border: 1px solid rgba(0, 255, 255, 0.2);
  padding: 15px;
  box-shadow: 0 0 10px rgba(0, 255, 255, 0.1);
  display: flex;
  flex-direction: column;
}

.panel-header {
  font-weight: bold;
  border-bottom: 1px solid var(--hud-color);
  padding-bottom: 5px;
  margin-bottom: 15px;
  letter-spacing: 2px;
}

/* 파동 애니메이션 (네바와 마리의 융합) */
.wave-container {
  height: 100px;
  position: relative;
  overflow: hidden;
  margin-bottom: 20px;
  border: 1px dashed rgba(0, 255, 255, 0.3);
}

.wave-line {
  position: absolute;
  top: 50%;
  width: 200%;
  height: 2px;
  opacity: 0.7;
}

.wave-neva {
  background-color: var(--alert-color);
  /* 사인파 형태를 CSS로 근사하거나 SVG 사용 권장 */
  animation: waveMove 3s infinite linear, glitch 5s infinite; 
}

.wave-marie {
  background-color: var(--hud-color);
  animation: waveMove 4s infinite linear reverse;
}

.merge-conflict-warning {
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  color: var(--alert-color);
  font-weight: bold;
  font-size: 0.8rem;
  opacity: 0;
  animation: flash 4s infinite;
}

/* 데이터 블록 및 리스트 */
.data-block p, .task-list li {
  margin: 5px 0;
  font-size: 0.9rem;
}

.task-list {
  list-style: none;
  padding: 0;
}

.task-done { color: rgba(0, 255, 255, 0.5); text-decoration: line-through; }
.task-active { color: #fff; text-shadow: 0 0 5px var(--hud-color); }
.task-wait { color: gray; }

.text-alert { color: var(--alert-color); }
.text-green { color: #0f0; }
.blink { animation: blinker 1s linear infinite; }

/* 중앙 타겟팅 UI */
.center-view {
  flex-grow: 1;
  display: flex;
  align-items: center;
  justify-content: center;
  position: relative;
}

.targeting-reticle {
  width: 300px;
  height: 300px;
  border: 1px solid rgba(0, 255, 255, 0.3);
  border-radius: 50%;
  position: relative;
  animation: rotateSlow 20s infinite linear;
}

.crosshair {
  position: absolute;
  top: 50%;
  left: 50%;
  width: 20px;
  height: 20px;
  border: 1px solid var(--hud-color);
  transform: translate(-50%, -50%);
}
.crosshair::before, .crosshair::after {
    content:'';
    position: absolute;
    background: var(--hud-color);
}
.crosshair::before { top: 50%; left: -10px; right: -10px; height: 1px; }
.crosshair::after { left: 50%; top: -10px; bottom: -10px; width: 1px; }


.reticle-text {
  position: absolute;
  bottom: -30px;
  left: 50%;
  transform: translateX(-50%);
  font-size: 0.8rem;
  letter-spacing: 3px;
  animation: blinker 2s infinite;
}

/* 버튼 및 링크 */
.hud-btn {
  display: block;
  color: var(--hud-color);
  text-decoration: none;
  margin: 5px 0;
  padding: 5px;
  border: 1px solid transparent;
  transition: all 0.3s;
}

.hud-btn:hover {
  background-color: rgba(0, 255, 255, 0.2);
  border: 1px solid var(--hud-color);
  padding-left: 10px; /* 호버 시 화살표 이동 효과 */
}

/* 타이핑 효과 */
.typewriter-text p {
  overflow: hidden; /* Ensures the content is not revealed until the animation */
  border-right: .15em solid var(--hud-color); /* The typwriter cursor */
  white-space: nowrap; /* Keeps the content on a single line */
  margin: 0 auto; /* Gives that scrolling effect as the typing happens */
  letter-spacing: .15em; /* Adjust as needed */
  animation: 
    typing 3.5s steps(40, end),
    blink-caret .75s step-end infinite;
}

/* Animations */
@keyframes blinker { 50% { opacity: 0; } }
@keyframes flash { 0%, 90% { opacity: 0; } 95% { opacity: 1; } 100% { opacity: 0; } }
@keyframes rotateSlow { 100% { transform: rotate(360deg); } }
@keyframes waveMove { 100% { transform: translateX(-50%); } }
@keyframes glitch {
  2%, 64% { transform: translate(2px,0) skew(0deg); }
  4%, 60% { transform: translate(-2px,0) skew(0deg); }
  62% { transform: translate(0,0) skew(5deg); }
}
@keyframes typing {
  from { width: 0 }
  to { width: 100% }
}
@keyframes blink-caret {
  from, to { border-color: transparent }
  50% { border-color: var(--hud-color); }
}

</style>
